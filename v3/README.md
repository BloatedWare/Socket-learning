# Version 3 (Domain Name Architecture) — Coming Soon

This directory is a placeholder for the upcoming domain-name capable version of the chat application.

### What's planned:
* Updating the client to accept web domain names (like `mychatserver.com` or `localhost`) instead of just raw IP addresses.
* Upgrading the underlying network logic to resolve URLs so users can connect from anywhere on the web.

*Check back soon for updates! For the current working version, please head over to the [v1/](../v1/) directory.*